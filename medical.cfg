base_power = 2.0;

device = @{
    modes = (1.0, 2.0, 3.0);
    max_power = $* base_power 3.0$;
    thresholds = @{
        min = 0.5;
        max = 5.0;
    };
};

modes = (1.0, 2.0, 3.0);
thresholds = @{
    min = 0.5;
    max = 5.0;
};
